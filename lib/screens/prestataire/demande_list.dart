import 'package:flutter/material.dart';

class DemandeList extends StatelessWidget {
const DemandeList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: Text("Demande List"),
    );
  }
}