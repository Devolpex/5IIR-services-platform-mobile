import 'package:flutter/material.dart';

class PropositionList extends StatelessWidget {
const PropositionList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container( 
      child: Text("Proposition List"),
    );
  }
}